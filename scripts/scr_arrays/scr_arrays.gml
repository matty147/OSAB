/// @description checks if the id is available for the object is available
/// @param id string/numb of you want to check
/// @param array the array to check
/// @returns bolean
function check_array_id_availability(id,array)
{
    for (var i = 0; array_length(array);i++)
    {
        
    }
}